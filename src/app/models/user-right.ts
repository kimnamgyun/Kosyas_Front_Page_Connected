export class UserRight {
    /**
     * seq
     */
    seq: number;
    /**
     * 아이디. 이메일 (사용자등록 폼)
     */
    id: string;
    /**
     * 비밀번호 (사용자등록 폼)
     */
    pw: string;
    /**
     * 비밀번호 확인(사용자등록 폼)
     */
    pw_t: string;
    /**
     * 이름 (사용자등록 폼)
     */
    nm: string;
    /**
     * 휴대폰 (사용자등록 폼)
     */
    phone: string;
    /**
     * 회사명 (사용자등록 폼)
     */
    comp_nm: string;
    /**
     * 국가 (사용자등록 폼)
     */
    nat_cd: string;
    /**
     * 권한 (사용자등록 폼)
     */
    lvl: number;
}
